# Database
# DATABASE_URL=postgresql://user:password@localhost/familyguard

# Security (Run `openssl rand -hex 32` or similar to generate a strong key)
SECRET_KEY=replace_this_with_a_long_random_string_in_production
